box: pjvds/golang
build:
  steps:
    -  script:
        name: go get
        code: |-
          pwd
          go get ./src/signaling/
    -  script:
        name: go build
        code: |-
          go build
    -  script:
        name: go test
        code: |-
          go test -v ./src/signaling/
